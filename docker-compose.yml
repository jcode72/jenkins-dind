version: "3"
services:
  jenkins: 
    build: .
    privileged: true
    volumes:
      - ./plugins:/var/jenkins_home/plugins
    environment:
      JAVA_OPTS: "-Djava.awt.headless=true"
    ports:
      - 50000:50000
      - 8080:8080
